const gameboard = require('./gameboard')

test.todo('able to place ships at coordinates by calling ship()')

test.todo('receiveAttack take a pair of coordinates and sends a determines whether or not a ship was hit')
test.todo('keeps track os missed attacks so they display properly')